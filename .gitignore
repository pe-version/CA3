# Terraform
terraform/.terraform/
**/.terraform/
.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/tfplan
terraform/*.tfplan
terraform/terraform.tfvars.bak

# macOS
.DS_Store
**/.DS_Store

# Backup files
*.bak
*.backup

# Sensitive
terraform/terraform.tfvars  # Contains your IP and potentially sensitive config

# Planning documents (personal use, don't commit)
.grade-improvement-plan.md
.quick-wins-no-video.md
.hpa-full-scaling-optionb.md

# Video files (too large for GitHub, submit separately)
evidence/*.mov
evidence/resilience-test-video.mp4
*.mov
# Allow compressed backup video
!evidence/resilience-test-video-compressed.mp4
